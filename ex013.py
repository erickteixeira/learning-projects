si = float(input('Qual é o seu salário inicial ? : '))
a = float(input('Qual é o valor do aumento que você irá receber ? : '))

sf = si * (100 + a)/100

print('Seu salário com {} de aumento ficará {}'.format(a,sf))

